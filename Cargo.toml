[package]
name = "spa-freebsd-oss"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc   = "0.2.186"
libspa = "0.9.2"
nix    = { version = "0.30.1", features = ["ioctl"] } # same version as in libspa's dependencies
regex  = "1.12.4"
uds    = "=0.4.2"
